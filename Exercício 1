#include <stdio.h>
#include <stdlib.h>

int main(){
    int a, b, i;
    printf("Coloque o intervalo da multiplicacao: ");
    scanf("%d%d", &a, &b);
    for (i=a; i<=b; i++){
        if ((i%9==0) || (i%19==0)){
            printf("%d\t", i);
        }
    }
return 0;
}
